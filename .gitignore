node_modules
settings.json
.DS_Store
images
uploads
config.env
.env
token.json

# Google OAuth credentials - never commit
credentials.json
*-todo.md
*.txt

# Resources
MD/
AGENT/

# Server configuration files with sensitive data - never commit
.htaccess

# Deployment scripts - server-specific, don't commit (contain sensitive data)
check-google-photos-server.sh
add-env-vars-to-staging-htaccess.sh
add-google-oauth-to-staging-htaccess.sh
update-google-oauth-env.sh
upload-google-oauth-env.sh
upload-google-photos-files.sh

# SQLite database files (but not data directory)
data/*.db

# Google Search Console coverage reports
*Coverage*.zip
*Coverage*.csv
*.csv
Chart.csv
Metadata.csv
"Critical issues.csv"
"Non-critical issues.csv"
